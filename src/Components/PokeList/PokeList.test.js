// import { render } from '@testing-library/react';
// import PokeList from './PokeList';

// test('renders the pokemon name', () => {
//   const container = render(<PokeList poke="name" />);
//   expect(container).toMatchSnapshot();
// });
